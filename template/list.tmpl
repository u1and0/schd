<!DOCTYPE html>
<html lang="ja">
<head>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
  <link rel="stylesheet" href="static/style.css">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
</head>
<style>
.konpo {
    background-color: #FFA500;
}
.syuka {
    background-color: #1E90FF;
}
.noki {
    background-color: #999999;
}
</style>
<html>
  <h3>生産番号 カレンダー表示</h3>
  <table>
  <!-- <table class="table table-striped"> -->
    <thead>
      <tr>
        <th>日付</th>
        <th class="konpo">梱包 機器名</th>
        <th class="konpo">梱包 生産番号</th>
        <th class="konpo">梱包 担当者</th>
        <th class="konpo">梱包 梱包会社依頼要否</th>
        <th class="konpo">梱包 外寸法(mm)</th>
        <th class="konpo">梱包 質量(kg)</th>
        <th class="konpo">梱包 輸送手段</th>
        <th class="konpo">梱包 到着予定日</th>
        <th class="konpo">梱包 問合わせ番号</th>
        <th class="konpo">梱包 備考</th>

        <th class="syuka">出荷 機器名</th>
        <th class="syuka">出荷 生産番号</th>
        <th class="syuka">出荷 担当者</th>
        <th class="syuka">出荷 備考</th>

        <th class="noki">納期 機器名</th>
        <th class="noki">納期 生産番号</th>
        <th class="noki">納期 担当者</th>
        <th class="noki">納期 備考</th>
      <tr>
    </thead>
    <tbody>
    {{range .r}}
    <tr>
      <td>{{.Date.Format "2006-01-02"}} {{ .Date.Weekday }}</td>
      <td class="konpo">{{ .KonpoName }}</td>
      <td class="konpo"><a href=/api/v1/view/{{.KonpoID}}>{{.KonpoID}}</a></td>
      <td class="konpo">{{ .KonpoAssign }}</td>
      <td class="konpo">{{ .Irai }}</td>
      <td class="konpo">{{ .WDH }}</td>
      <td class="konpo">{{ .Mass }}</td>
      <td class="konpo">{{ .Yuso }}</td>
      <td class="konpo">{{ .Chaku.Format "2006-01-02" }}</td>
      <td class="konpo">{{ .ToiawaseNo }}</td>
      <td class="konpo">{{ .KonpoMisc }}</td>

      <td class="syuka">{{ .SyukaName }}</td>
      <td class="syuka"><a href=/api/v1/view/{{.SyukaID}}>{{.SyukaID}}</a></td>
      <td class="syuka">{{ .SyukaAssign }}</td>
      <td class="syuka">{{ .SyukaMisc }}</td>

      <td class="noki">{{ .NokiName }}</td>
      <td class="noki"><a href=/api/v1/view/{{.NokiID}}>{{.NokiID}}</a></td>
      <td class="noki">{{ .NokiAssign }}</td>
      <td class="noki">{{ .NokiMisc }}</td>

    </tr>
    {{end}}
    <tbody>
  </table>

</html>
